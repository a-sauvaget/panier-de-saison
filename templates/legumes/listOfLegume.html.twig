<!-- HERO -->
<section class="hero hero--custom has-text-centered pt-5">
	<div class="hero-body">
		<h2 class="subtitle">Les légumes du mois de:</h2>
		<h1 class="title title--hero is-uppercase"></h1>
	</div>
</section>
<!-- SECTION  -->
<section class="section is-flex is-flex-wrap-wrap is-justify-content-space-evenly section-legumes">
	{% for legume in legumes %}
		<div class="card mb-4">
			<a href="/legumes/culpa" class="box products-box">
				<div class="card-image">
					<figure class="image is-4by3">
						<img src="/uploads/product/{{legume.file}}" alt="{{legume.legumeName}}">
					</figure>
				</div>
				<h2 class="title pt-5 has-text-centered products-title">{{legume.legumeName}}</h2>
			</a>
		</div>
	{% endfor %}

</section>
